version: 2.1

orbs:
  bun: cmgriffing/bun-orb@0.0.2

jobs:
  build:
    docker:
      - image: cimg/base:2023.03
    steps:
      - checkout
      - run: cd apps/git-auth
      - run: bun bake
# Orchestrate our job run sequence
workflows:
  build:
    jobs:
      - bun/setup
      - build
