version: 2.1

orbs:
  bun: cmgriffing/bun-orb@0.0.13
  node: circleci/node@5.1.0

jobs:
  build:
    docker:
      - image: cimg/node:18.17
    steps:
      - checkout
      - bun/setup
      - run: cd apps/git-auth && bun bake
# Orchestrate our job run sequence
workflows:
  build:
    jobs:
      - build
